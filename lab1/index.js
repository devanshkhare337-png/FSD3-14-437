// write a function to take your name and print with hello in it
function style(name) {
    console.log(`Hello, ${name}!`);
}

style("Devansh");

const sayit = (name) => {
    console.log(`Hello, ${name}!`);
};
// take two argument and find their sum

const sum = (a, b) => {
    return a + b;
}